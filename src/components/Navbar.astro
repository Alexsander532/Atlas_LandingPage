<header class="site-header" id="navbar">
    <a class="brand" href="#home" aria-label="Atlas, início">
        <span class="brand-mark" aria-hidden="true">
            <i class="brand-book brand-book--green"></i>
            <i class="brand-book brand-book--orange"></i>
            <i class="brand-book brand-book--blue"></i>
        </span>
        <span>Atlas</span>
    </a>

    <nav class="nav-pills" aria-label="Navegação principal">
        <a class="nav-link is-active" href="#home">Início</a>
        <a class="nav-link" href="#resources">Recursos</a>
        <a class="nav-link" href="#testimonials">Depoimentos</a>
        <a class="nav-link" href="#resources">FAQ</a>
    </nav>

    <div class="nav-actions">
        <a class="language-switch" href="#home" aria-label="Português do Brasil"><span>◉</span> BR</a>
        <a class="nav-download" href="#resources">Baixar agora</a>
    </div>
</header>

<style>
    .site-header { position: absolute; z-index: 20; top: 0; left: 50%; display: flex; align-items: center; justify-content: space-between; width: min(1370px, calc(100% - 10rem)); padding-top: 2.2rem; transform: translateX(-50%); }
    .brand { display: flex; align-items: center; gap: .8rem; color: #f7f8fb; font-size: 2.1rem; font-weight: 800; letter-spacing: -.055em; }
    .brand-mark { position: relative; display: block; width: 40px; height: 39px; }
    .brand-book { position: absolute; display: block; width: 19px; height: 28px; border: 2px solid rgba(4, 22, 30, .45); border-radius: 2px 3px 3px 2px; box-shadow: inset -3px 0 rgba(255,255,255,.22); transform: skewY(-3deg); }
    .brand-book--green { left: 0; top: 3px; background: #42c36a; }
    .brand-book--orange { left: 10px; top: 7px; background: #f38437; }
    .brand-book--blue { left: 18px; top: 11px; background: #3b9df2; }
    .nav-pills { display: flex; gap: .15rem; padding: .35rem .45rem; border: 1px solid rgba(173, 201, 226, .15); border-radius: 99px; background: rgba(20, 29, 37, .62); box-shadow: inset 0 1px rgba(255,255,255,.04), 0 10px 30px rgba(0,0,0,.13); backdrop-filter: blur(16px); }
    .nav-link { padding: .72rem 1.45rem; border-radius: 99px; color: #bdc8d6; font-size: .95rem; transition: color .2s ease, background .2s ease; }
    .nav-link:hover, .nav-link.is-active { color: #fff; background: rgba(5, 10, 15, .7); }
    .nav-actions { display: flex; align-items: center; gap: 1.4rem; }
    .language-switch { display: flex; align-items: center; gap: .7rem; padding: .82rem 1.3rem; border: 1px solid rgba(173, 201, 226, .13); border-radius: 99px; color: #f1f4f8; font-size: .93rem; background: rgba(14, 20, 26, .38); }
    .language-switch span { display: grid; place-items: center; width: 19px; height: 19px; border: 1px solid #f1f4f8; border-radius: 50%; color: #dce9f5; font-size: .6rem; }
    .nav-download { padding: .98rem 2.1rem; border-radius: 99px; color: #fff; background: linear-gradient(135deg, #137dec, #168ef9); box-shadow: 0 9px 20px rgba(13, 121, 234, .24); font-size: .95rem; font-weight: 600; transition: transform .2s ease, box-shadow .2s ease; }
    .nav-download:hover { transform: translateY(-2px); box-shadow: 0 12px 24px rgba(13, 121, 234, .38); }

    @media (max-width: 1180px) {
        .site-header { width: min(1100px, calc(100% - 4rem)); }
        .nav-link { padding-inline: .85rem; }
        .nav-actions { gap: .6rem; }
        .language-switch { padding-inline: .85rem; }
        .nav-download { padding-inline: 1.25rem; }
    }

    @media (max-width: 900px) {
        .site-header { width: calc(100% - 2rem); padding-top: 1.4rem; }
        .brand { font-size: 1.65rem; }
        .brand-mark { transform: scale(.8); transform-origin: left center; margin-right: -.35rem; }
        .nav-pills { display: none; }
        .language-switch { display: none; }
        .nav-download { padding: .78rem 1rem; font-size: .8rem; }
    }
</style>
